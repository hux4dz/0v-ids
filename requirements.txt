# Core dependencies
psutil>=5.9.0  # System and process utilities for monitoring network connections, CPU, memory usage, and process information
scapy>=2.5.0  # Powerful packet manipulation library for network scanning, packet crafting, and network discovery
netifaces>=0.11.0  # Cross-platform library for retrieving network interface information and IP addresses
python-dotenv>=1.0.0  # Load environment variables from .env files for secure configuration management

